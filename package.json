{
  "name": "licitaciones-intelligence",
  "version": "1.0.0",
  "description": "Sistema modular de inteligencia de licitaciones - Engine + API + Frontend",
  "private": true,
  "scripts": {
    "build:engine": "cd engine && npm run build",
    "build:api": "cd api && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:engine && npm run build:api && npm run build:frontend",
    "dev:api": "cd api && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "cd api && npm start"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
